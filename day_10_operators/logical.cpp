#include<iostream>
using namespace std;

// int main() {
//    1. Logical AND (&&) example
//     int a = 5, b = 10;
//     bool result = (a < b) && (b > a);
//     cout << "Result of (a < b) && (b > a): " << result << endl; // Output: 1 (true)
//     return 0;

    // 2. Logical OR (||) example
    // int a = 5, b = 10;
    // bool result = (a > b) || (b > a);
    // cout << "Result of (a > b) || (b > a): " << result << endl; // Output: 1 (true)
    // return 0;

    //3. Logical NOT (!) example
    // int a = 5, b = 10;
    // bool result = !(a > b);
    // cout << "Result of !(a > b): " << result << endl; // Output: 1 (true)
    // return 0;
      
// }
